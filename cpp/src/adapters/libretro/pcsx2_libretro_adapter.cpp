#include "pcsx2_libretro_adapter.h"

// Skeleton phase: no out-of-line overrides yet. This translation unit
// exists so Qt's AUTOMOC sees the header and emits moc_pcsx2_libretro_adapter.cpp,
// which provides the Q_OBJECT vtable. Later sub-projects will fill in
// settingsSchema / controllerBindingDefsForType / pathsDefs / extractSerial /
// findResumeFile / raConsoleId here.
